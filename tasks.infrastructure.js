var infrastructureTasks = {
 
    buildLinks: function(p_room) {
        // TODO: Fill in earlier levels.
        
        if (p_room.controller.level == 4) {
            
        }
    }
}

module.exports = infrastructureTasks;